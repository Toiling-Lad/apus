sudo: false
language: node_js
node_js:
  - "stable"
cache:
  directories:
    - node_modules
install:
  - npm install
script:
  - npm test
  - npm login --username $EXPO_USERNAME --password $EXPO_PASSWORD
  - npm publish