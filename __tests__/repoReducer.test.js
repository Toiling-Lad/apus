
import repoReducer, {initialState, GET_REPOS, GET_REPOS_SUCCESS, GET_REPOS_FAIL} from '../reducers/repoReducer';

 

// it('returns the same state on an unhandled action', () => {
//   expect(repoReducer(initialState, {type: '_NULL'})).toMatchSnapshot();
// });

// Thease need to be fixed appropriately.
// it('handles GET_REPOS action', () => {
//   expect(repoReducer(initialState, GET_REPOS)).toMatchSnapshot();
// });

// it('handles GET_REPOS_SUCCESS action', () => {
//   expect(repoReducer(initialState, GET_REPOS_SUCCESS)).toEqual({
//     repos: []
//   });
// });
  
// it('handles GET_REPOS_FAIL action', () => {
//   expect(repoReducer(initialState, GET_REPOS_FAIL)).toEqual({
//     repos: []
//  });;
// });


